source "https://rubygems.org"

# Local-only Gemfile: used just to preview the site. GitHub Pages builds the site
# itself using its own pinned Jekyll version — we don't need github-pages here.
gem "jekyll", "~> 4.3"

group :jekyll_plugins do
  gem "jekyll-sitemap"
end
